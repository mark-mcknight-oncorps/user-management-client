import { EnvironmentConfig } from "./environment-config.interface";
import { environment as environment_local } from './environment.local'

export const environment: EnvironmentConfig = environment_local;